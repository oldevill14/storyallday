'use client';

import { StudioWorkspace } from '@/components/studio/StudioWorkspace';

// "สตูดิโอละครสั้น" — general vertical short-drama studio (original).
export default function StudioPage() {
  return <StudioWorkspace mode="drama" />;
}
