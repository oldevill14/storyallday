'use client';

import { StudioWorkspace } from '@/components/studio/StudioWorkspace';

// "สร้างคลิปขายของ" — sales-focused: product + reusable cast + selling style.
export default function SalesClipPage() {
  return <StudioWorkspace mode="sales" />;
}
